# Leave empty to run the site fully on deterministic seed data (zero-credential boot).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Cloudflare Turnstile widget site key. This key is public by design.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
